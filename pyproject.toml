[project]
name = "browser-eng"
version = "0.1.0"
description = "Building a Browser"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
[project.scripts]
browser = "src.browser.main:main"
